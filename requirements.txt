# HTTP / download
curl_cffi>=0.7
tqdm>=4.66

# PDF parsing
pymupdf>=1.27

# Indexação (embeddings + vector store + sparse)
FlagEmbedding>=1.3       # bge-m3 (dense + sparse + colbert num único forward)
sentence-transformers>=3.0
qdrant-client>=1.12
rank_bm25>=0.2.2
torch>=2.2               # CPU/CUDA/MPS autodetect
# IMPORTANTE: no Windows o `pip install torch` puro instala a build CPU-only.
# Para usar GPU NVIDIA na indexação, instale ANTES do requirements:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# Linux/Mac: o pip já resolve a build certa automaticamente.
numpy>=1.26

# Geração + avaliação (LLM-as-judge usa o mesmo SDK)
anthropic>=0.40
# Nota: Ragas 0.2+ tem breaking change na API que requer InstructorLLM (apenas OpenAI).
# Substituímos Ragas por LLM eval simples via Claude Haiku 4.5 — ver eval/evaluate.py.

# Serving (opcional)
fastapi>=0.115
uvicorn>=0.30
streamlit>=1.40
