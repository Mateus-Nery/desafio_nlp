# Fase 1 — Ingestão
curl_cffi>=0.7
tqdm>=4.66

# Fase 2 — Parser
pymupdf>=1.27

# Fase 4 — Indexação
FlagEmbedding>=1.3       # bge-m3 (dense + sparse + colbert num único forward)
sentence-transformers>=3.0
qdrant-client>=1.12
rank_bm25>=0.2.2
torch>=2.2               # CPU/CUDA/MPS autodetect
numpy>=1.26

# Fase 6 — Geração
anthropic>=0.40

# Fase 7 — Avaliação
ragas>=0.2

# Fase 8 — Serving (opcional)
fastapi>=0.115
uvicorn>=0.30
streamlit>=1.40
